##视差贴图

这个贴图和法线贴图很相像，但是性能消耗更多，当然，效果也更好些
他可以和法线贴图一起用，效果相当好
区别于法线贴图是对于法线的移动，而视差贴图是直接对于物体的顶点进行位移
让他们的顶点之间直接有了高度差
所以很必要的点是必须这个物体需要有很多的顶点

视差贴图的顶点着色器和法线贴图的顶点着色器差不多，但需要把position 和在切线空间里面的摄像机的位置viewPos发给片段着色器

陡峭视差映射
这个东西是更加精准的视差贴图映射算法
是将总额的深度范围划分成同一个深度or高度的一个个层面，
我们在这会沿着视觉方向来移动采样坐标，一直采样到采样到低于当前层的深度，停止
当这个层的采样的深度值小于深度贴图的值的时候，意味着这一层的的视线向量部分处于物体的表面之下，此时就找到地方了
以这个地方作为真正采样的深度值
当然这个方法也有局限，层与层之间的距离过大，会导致精度缺失，而层与层之间的距离过小，会使得运算量倍增，性能开销过大
具体实践方法是
用float定义层数的数量
计算每一层的深度（1/层数）
计算纹理坐标偏移 （用视线方向的xy坐标乘以   再除以需要的层数）
然后查找深度贴图上的纹理坐标，获取他的r通道
之后 循环遍历 （就是对比视线到达这个层时的高度与此时实际贴图在这个层的高度，当视线获得的高度比贴图实际高度小的时候结束）
直到沿着P¯向量找到第一个返回低于（位移）表面的深度的纹理坐标偏移量
。从fragment的纹理坐标减去最后的偏移量，来得到最终的经过位移的纹理坐标向量。

意思就是反复检测，在视线向量没有与起伏的边界相交的时候，每过一层就偏移一份纹理坐标
当你的视线向量第一次接触到物体的时候，此时就是最终得出的纹理坐标结果了

这里有一个优化的小巧思
在声明layers的时候，可以根据不同的视角安排不同的样本数量，以减小开销
核心思想是当你垂直的时候你的样本数量就减少一点，因为此时并不需要那么多
而你斜着看的时候就可以增加样本数量，提升采样精度
可以使用
线性插值方法，设置一个最大层数和最小层数，让z方向的（0，0，1）（也就是切线空间的法线）向量点积视线角度并且取绝对值
这样就可以实现动态变化插值，动态改变样本数量

##视差遮蔽映射，原则和陡峭视差映射的原理相同，也是使用插值在视线向量和起伏边界进行运算
也就是说获取相交前前一个点和相交后的第一个点来进行插值，以达到获取最终纹理坐标的目的
 这一步也就是做完陡峭视差映射之后的下一步了




