##泛光（辉光）

泛光是一种后期处理效果，会使得场景里面部分不是光源的地方也会有光
应该叫光晕效果

HDR和泛光是不同的东西，他们是完全不同的技术
可以只用hdr但是不用泛光，也可以只用泛光
也可以一起用

直接拿场景举例子
将带有颜色的发光立方体渲染的时候，需要渲染两次帧缓冲
获取此时一张帧缓冲
然后将超过某种亮度界限的像素进行模糊
可以是自定义的值，通常hdr会很满足
可以通过调整模糊的范围和强度来决定泛光的强度

此时拿到那张模糊光源的帧缓冲，然后叠加在原来的场景上
此时泛光就完成了，光源边缘就是糊糊的

详细描述是
