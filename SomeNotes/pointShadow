ok这里是点光源的一点学习笔记
算是记录下在点光源这一节我记忆比较深刻的地方

点光源的算法也和定向阴影差不多
依旧是从光源视角生成新的深度贴图，基于当前摄像机位置进行深度贴图采样
然后比较两次深度贴图的数值
不同之处在于 定向阴影映射是生成一张2d的深度贴图
而点光源要生成一个深度立方体贴图 也就是一口气生成六张
所以得一口气生成六次立方体贴图 等于需要六个不同的视图矩阵
当然，很浪费性能
所以伟大的前辈选择了小巧思
即先创建一个立方体贴图 
for (GLuint i = 0; i < 6; ++i)
glTexImage2D(GL_TEXTURE_CUBE_MAP_POSITIVE_X + i, 0, GL_DEPTH_COMPONENT, 
SHADOW_WIDTH, SHADOW_HEIGHT, 0, GL_DEPTH_COMPONENT, GL_FLOAT, NULL);
发现一个很妙的写法 就是这个GL_TEXTURE_CUBE_MAP_POSITIVE_X + i 
它是用于切换正方体代表的每个面的 当然
必须使用GL_TEXTURE_CUBE_MAP_POSITIVE_X作为起点

6个正方体贴图都要使用不同的观察矩阵，所以需要手搓六个不同的矩阵
不过好在大体都是一样的 咱们可以用一个vecotor数组来存储这六个矩阵

渲染这个阴影的时候需要用到几何着色器，这个几何着色器的思路
是
一次接受3个点，最多接受18个顶点：对应着
