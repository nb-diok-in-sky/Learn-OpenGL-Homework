##延迟着色法

目前一直使用的光照方式其实叫做：正向渲染 或者说正向着色法
意思是先计算一个物体的光照，再渲染下一个物体的渲染。一个一个，等物体多了的话就很恼火，对于
性能的影响特别大


所以现在引入了延迟着色法（延迟渲染）

延迟渲染法则就是推迟大部分计算量非常大的渲染（比如光照）到后期再进行的想法

它包含了两个处理阶段：第一个几何处理阶段里：我们先渲染场景一次，之后再
获取对象的各种集合信息，并且存储再一系列在G缓冲里面，包括了
位置向量，颜色向量，法向量或者镜面值，场景中储存这些在G缓冲的几何信息会拿去制作更复杂的光照计算

第二个光照处理阶段中会使用G缓冲内部的纹理数据，在光照处理中先渲染一个屏幕大小的方形，
然后使用G缓冲的几何数据对每一个片段计算场景的光照
在这个阶段，G缓冲会替代原本顶点着色器传递信息的功能，片段着色器将会从顶点着色器里面获取变量

这种方法的好处是在光照处理阶段中处理的每一个像素都只会处理一次，，可以节省很多无用的渲染调用，并且可以进行别的都优化以此达到渲染更多的目的

##G缓冲
G缓冲是永对所有用来储存光照相关的数据，并且在最后的光照处理里面使用的所有的纹理的总能工程

G缓冲可以用来存储
-一个3D位置向量来计算片段位置以供lightDir和viewDir来使用
-一个RGB漫反射颜色向量（反照率）
-一个用来判断平面斜率的3D法向量
-一个镜面强度（浮点值）
-所有光源的位置和颜色向量
-玩家或者观察者的位置向量
等等等等等
纹理里面什么东西都可以存储
此时G缓冲里面存储的就已经是所有的信息经过计算之后的最终版本了，进入光照处理阶段
此时会渲染一个2D全屏的方形，在每个效率上运行频段着色器进行信息获取

延迟着色法有个缺陷是他无法进行混合，所以我们引入了正向渲染与延迟渲染相结合的方式

##结合延迟渲染和正向渲染

在延迟渲染后，需要复制出在集合渲染阶段储存的深度信息，然后输出到默认帧缓冲的深度缓冲
之后拿去渲染就行了
但是很可惜的是还没有完，如果此时我们仔细观察会发现，所有的光源都是会像同一个光源一样没有衰减

所以我们选择使用glBlitFramebuffer多复制一个帧缓冲区域
随后指定其中一个帧缓冲为读帧缓冲
另外一个为写帧缓冲




